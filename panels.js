export function createLogger(container){return message=>{const line=document.createElement("div");line.className="log-line";line.innerHTML=`<span class="log-time">${new Date().toLocaleTimeString()}</span>${escapeHtml(message)}`;container.prepend(line);while(container.children.length>120)container.removeChild(container.lastChild)}}export function renderSelected(container,c){if(!c){container.className="selected-empty";container.textContent="Click a creature in the simulation.";return}container.className="creature-card";container.innerHTML=[["ID",`#${c.id}`],["Type",c.type],["Energy",c.energy.toFixed(1)],["Age",c.age.toFixed(1)],["Speed gene",c.genes.speed.toFixed(2)],["Sight gene",c.genes.sight.toFixed(1)],["Metabolism",c.genes.metabolism.toFixed(3)],["Size",c.genes.size.toFixed(2)]].map(([k,v])=>`<div class="row"><span>${k}</span><strong>${v}</strong></div>`).join("")}function escapeHtml(s){return String(s).replace(/[&<>"']/g,ch=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[ch]))}
